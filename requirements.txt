# SenseOptics Tester — single-file build (минимальные зависимости)
# scipy НЕ нужен (заменён numpy-эквивалентом внутри app.py).
streamlit>=1.36,<2
opencv-python-headless>=4.10,<5
numpy>=1.26,<3
pillow>=10.0,<13
# matplotlib опционален: нужен только для 3 диагностических графиков.
# Если упрётесь в лимит диска free-плана — удалите строку ниже,
# приложение продолжит работать (оверлеи на OpenCV останутся, графики отключатся).
matplotlib>=3.8,<4
